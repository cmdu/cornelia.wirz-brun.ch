$( "a" ).each(function( index ) {
$( this ).css("border-bottom", "2px dashed " + randomColor());
});

$( "#spezi" ).each(function( index ) {
$( this ).css("border-bottom", "2px solid " + randomColor());
});